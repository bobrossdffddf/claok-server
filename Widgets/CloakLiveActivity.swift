// The Live Activity's presentation lives next door in DriveLiveActivity.swift.
// Keeping one file per widget, named after the widget it declares, is what
// stops a second `ActivityConfiguration` for the same attributes appearing by
// accident, which iOS resolves by picking one of them at random.
//
// This file is kept, empty, only so the target's file list does not change.
